# Rails essentials
/.bundle
/.env*
!/.env.example

# Logs and temp files
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Storage and uploads
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep
/public/uploads

# Compiled assets (only temp/cache files to prevent breaking project after clone)
/public/assets/*.tmp
/public/assets/*.cache
/public/assets/node_modules
/app/assets/builds/*.tmp
/app/assets/builds/*.cache
/app/assets/builds/node_modules
!/app/assets/builds/.keep
# Essential compiled files (CSS, JS, WASM, etc.) are tracked to prevent broken display

# Security
/config/master.key
/config/credentials/*.key

# Database
*.sqlite3
*.sqlite3-*

# Dependencies
node_modules/

# Test coverage
/coverage/